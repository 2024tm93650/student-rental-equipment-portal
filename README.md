```bash
# Clone the repository
git clone https://github.com/2024tm93650/student-rental-equipment-portal.git
cd student-rental-equipment-portal

# Install backend dependencies and run service
cd server
npm install
npm run dev

# Install frontend dependencies and run application
cd ../client
npm install
npm run start
```